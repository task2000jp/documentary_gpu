.mcp.json
.venv/
models/
build/
# 画像は最適化済み（scripts/optimize_images.py で計17MB）→ gitで配布。
#   Colabはclone一発で画像を入手（Drive不要）。
# BGM(42MB)/キャラクターは重い&著作権ありのため Drive 管理（Colabがセル4で取得）。
assets/bgm/
assets/characters/
.env
__pycache__/
*.pyc
.DS_Store

# OAuth/認証シークレット（絶対にコミットしない）
client_secret*.json
*.apps.googleusercontent.com.json
*credentials*.json
token.json
.clasp.json
