# ════════════════════════════════════════════════════════════
# Colab T4 環境用 — フルGPUスタック
# ════════════════════════════════════════════════════════════
# 方針: 画像生成・深度・パララックス・TTS・動画・GPU後処理を担う本番環境。
#       Colabには torch/CUDA が最初から入っているので torch は明示指定しない
#       （Colabのプリインストール版に合わせる。上書きするとCUDA不整合の元）。
#
#   Colabセル: !pip install -q -r requirements-colab.txt
# ════════════════════════════════════════════════════════════

# ── 深度・パララックス（Phase 1）──
transformers>=4.44          # Depth Anything V2
# depthflow                 # 任意: GPUシェーダーパララックス（pip install depthflow）

# ── 画像生成（Phase 2）──
diffusers>=0.31             # FLUX.1 schnell / SDXL / LTX-Video
accelerate
sentencepiece               # FLUX text encoder
bitsandbytes                # NF4量子化（FLUXをT4 15GBに載せる）

# ── ナレーション（Phase 3）──
# style-bert-vits2          # 日本語TTS第一候補（Colabで別途 pip install）
edge-tts                    # フォールバック
soundfile

# ── 動画生成（Phase 4・任意）──
imageio
imageio-ffmpeg

# ── ベクター図解・ヒーロー図（線/ノード/データ流アニメ）──
# A層 overlay_vector は Pillow のみで動く（追加依存なし）。
# C層 Manim ヒーロー図を使う時だけ ↓（apt: libcairo2-dev libpango1.0-dev も必要）
manim                       # background.type="manim" のヒーロー図カット用

# ── 音楽：理論based劇伴（MP1-MP3）──
music21                     # 作曲・MIDI生成
pedalboard                  # ミックス（reverb/EQ/comp・Spotify製）
pyloudnorm                  # -16 LUFS マスタリング
soundfile                   # wav I/O
# FluidSynth本体とSoundFontはapt: fluidsynth fluid-soundfont-gm
# AIテクスチャ(MP5・任意): stable-audio-tools / audiocraft

# ── 共通 ──
Pillow
numpy
