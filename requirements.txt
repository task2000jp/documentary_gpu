# ════════════════════════════════════════════════════════════
# 依存ライブラリは環境ごとに分離しています
# ════════════════════════════════════════════════════════════
#
#   ローカル（Intel Mac・設計/コーディング/軽量確認）:
#       .venv/bin/pip install -r requirements-local.txt
#       → torch等のGPUスタックは入れない
#
#   Colab T4（画像/深度/パララックス/TTS/動画の本番レンダリング）:
#       !pip install -q -r requirements-colab.txt
#       → torch/CUDAはColabプリインストール版を利用
#
# 重いGPU処理は全てColabで実行する設計のため、ローカルにtorchは不要。
# ════════════════════════════════════════════════════════════
